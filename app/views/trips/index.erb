
<div class='trip-index'>
<h1>Your trips</h1>

  <% @trips.each do |trip| %>
        <h3 id='trip-index'><%= link_to "#{trip.trip_name}", user_trip_path(@user, trip.id) %></h3>
  <% end %>

  <h2 id='add-trip'><%= link_to 'add a new trip', new_user_trip_path(@user.id)%></h2>
</div>
