
    var AccommodationView = function(accommmodation){
      this.model = accommodation;
      $('body').on('click', '#heartbeats', this.addHeart.bind(this));
    }

    AccommodationView.prototype = {

    
    }
